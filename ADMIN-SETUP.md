# 🔐 Налаштування системи адміністраторів

## 📋 Як налаштувати систему адміністраторів:

### 1. **Перший вхід (тільки для головного адміністратора):**
- Відкрийте: `http://192.168.0.101:8000/admin-login.html`
- Телефон: `+380688834630`
- Пароль: `bakery2024`
- **Творець системи:** @mm.slmn

### 2. **Додавання першого адміністратора:**
1. Увійдіть в адмін панель
2. Перейдіть на вкладку "Адміністратори"
3. Натисніть "Управління адміністраторами"
4. Додайте свій email як адміністратора

### 3. **Як додати інших адміністраторів:**
1. Користувач повинен зареєструватися на сайті
2. Увійдіть в адмін панель
3. Перейдіть в "Управління адміністраторами"
4. Введіть телефон користувача
5. Виберіть рівень доступу:
   - **Адміністратор** - повний доступ
   - **Модератор** - обмежений доступ

## 🛡️ Рівні доступу:

### **Адміністратор:**
- ✅ Редагування контенту
- ✅ Управління меню
- ✅ Налаштування сайту
- ✅ Додавання/видалення адміністраторів
- ✅ Доступ до всіх функцій

### **Модератор:**
- ✅ Редагування контенту
- ✅ Управління меню
- ✅ Налаштування сайту
- ❌ Додавання/видалення адміністраторів

## 🔍 Перевірка прав доступу:

### **На головному сайті:**
- Кнопка "Адмін" з'являється тільки для адміністраторів
- Звичайні користувачі не бачать цю кнопку

### **В адмін панелі:**
- Перевірка email та пароля
- Перевірка наявності в списку адміністраторів
- Обмеження доступу для модераторів

## 📁 Файли системи:

```
website/
├── admin-login.html      # Вхід для адміністраторів
├── admin.html            # Адмін панель
├── admin-users.html      # Управління адміністраторами
├── admin-styles.css      # Стилі адмін панелі
├── admin.js             # Функціонал адмін панелі
└── script.js            # Перевірка прав на головному сайті
```

## 🔧 Технічні деталі:

### **Збереження даних:**
- `localStorage.getItem('admins')` - список адміністраторів
- `localStorage.getItem('users')` - список користувачів
- `localStorage.getItem('currentUser')` - поточний користувач

### **Перевірка прав:**
```javascript
// Перевірка чи є користувач адміністратором
const admins = JSON.parse(localStorage.getItem('admins') || '[]');
const isAdmin = admins.find(a => a.phone === userPhone);
```

## 🚨 Безпека:

- **Локальне збереження** - дані зберігаються в браузері
- **Перевірка email** - тільки зареєстровані користувачі можуть стати адміністраторами
- **Рівні доступу** - різні права для різних ролей
- **Автоматичне приховування** - кнопка адміністратора з'являється тільки для авторизованих адміністраторів

## 💡 Поради:

1. **Регулярно перевіряйте** список адміністраторів
2. **Видаляйте** непотрібних адміністраторів
3. **Використовуйте різні рівні** доступу для різних людей
4. **Зберігайте резервні копії** важливих даних 